-  Data Types في C++ 🗂️
**الدليل الكامل من الصفر**
  
ملحوظة هامة  مشرحتهاش في الشيت الاول :

  لما تيجي تعرف متغير عرفه باس اسم يعجبك طالما مش محجوز في اللغة زي ما هنشرح الدرس ده مينفعش اسمي متغير بالاسماء دي او اسماء الطباعة و الادخال و ممنوع اكتب رقم ف الاول لكن في اي موضع ينفع عادي
  متاح ليك تبدا ب _ العلامة دي الاندرسكور عادي 
  .......
  GG
  يلا نبدا
\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\

لما بتكتب برنامج، بتقول للكمبيوتر "احفظ القيمة دي". بس الكمبيوتر بيسألك سؤال واحد قبل ما يعمل أي حاجة:

**"القيمة دي إيه بالظبط؟"**
مش لأنه غبي — لأنه محتاج يعرف يحجز كام مكان في الذاكرة، وازاي يتعامل مع الـسؤال ده bits اللي هيحطها جوه. الـ data type هو إجابة سؤال .

==================================================================================================================================================================
### الأعداد الصحيحة 🔢
أبسط نوع موجود. أرقام من غير فاصلة. الفرق الوحيد بين الأنواع هو **الحجم** — يعني أكبر رقم تقدر تحطه.
---

**`short`** — 2 bytes، بيشيل لحد ±32,767
```cpp
short x = 100;
cout << x;       // 100
```

---

**`int`** — 4 bytes، بيشيل لحد ±2,147,483,647

default — لو مش عارف تستخدم إيه، استخدم `int`.
مثال :
int age = 25;
cout << age;      // 25

cin >> age;       // بتدخل رقم من المستخدم
cout << age;      // بتطبع اللي دخله
```
---

**`long long`** — 8 bytes، بيشيل لحد ±9.2 × 10¹⁸

مثال :
long long bigNum = 9000000000LL;
cout << bigNum;    // 9000000000

cin >> bigNum;
cout << bigNum;
```
---

**`unsigned int`** — 4 bytes، من 0 لحد 4,294,967,295

بيلغي السالب ويضاعف الحد الأقصى.
مثال : 
unsigned int score = 4000000000;
cout << score;     // 4000000000

cin >> score;
cout << score;
```

> 💡 تجنب `long` لوحده — حجمه مش ثابت على كل الأنظمة. استخدم `long long` دايماً لو محتاج كبير.

=============================================================================================================================================================

### الأعداد العشرية 🔣

للأرقام اللي فيها فاصلة.

> ⚠️ الأعداد العشرية **مش دقيقة 100%**. `0.1 + 0.2` ممكن تطلع `0.30000000000000004`. ده مش bug — ده طبيعة تخزين الـ floating point في الـ hardware نفسه.

---

**`float`** — 4 bytes، دقة ~7 خانة عشرية

مثال :
float temp = 36.6f;
cout << temp;      // 36.6

cin >> temp;
cout << temp;
```



**`double`** — 8 bytes، دقة ~15 خانة عشرية

مثال :
double price = 99.99;
cout << price;     // 99.99

cin >> price;
cout << price;
```

**`long double`** — 8 أو 16 bytes حسب النظام، دقة ~18-19 خانة

مثال :
long double precise = 3.14159265358979323846L;
cout << precise;   // 3.14159265358979324

cin >> precise;
cout << precise;
=====================================================================================================================================================

### الحروف 🔤

**`char`** — 1 byte، حرف واحد بس

الطريف إنه بيتخزن فعلاً كـ **رقم**. الحرف `'A'` في الذاكرة هو الرقم 65. C++ بيعرضه كـ A لأنك قلتله إنه `char`.
```cpp
char letter = 'A';
cout << letter;    // A

cin >> letter;
cout << letter;
```

---

**`wchar_t`** — 2 أو 4 bytes، للغات زي العربي والصيني

لازم `L` قبل القيمة.
```cpp
wchar_t arabicChar = L'ز';
wcout << arabicChar;    // ز — لازم wcout مش cout
```

---

**`char16_t` و `char32_t`** — نسخ حديثة من Unicode بحجم ثابت على كل الأنظمة
```cpp
char16_t c1 = u'A';    // 2 bytes ثابت
char32_t c2 = U'A';    // 4 bytes ثابت
```

---

### الصح والغلط ✅

**`bool`** — 1 byte، قيمتين بس: `true` أو `false`
```cpp
bool isLoggedIn = true;
cout << isLoggedIn;    // 1  — بيطبع 1 مش true

bool hasError = false;
cout << hasError;      // 0

cin >> isLoggedIn;     // بتدخل 1 أو 0
cout << isLoggedIn;
```

> 💡 `cout` بيطبع `1` بدل `true` و `0` بدل `false`. ده طبيعي في C++.

==============================================================================================================================================================

### النصوص 📝

**`string`** — مش primitive type. ده class جاهز في مكتبة C++. محتاج `#include <string>` في أول الملف.
```cpp
#include <string>
using namespace std;

string name = "Ahmed";
cout << name;          // Ahmed

cin >> name;           // بياخد كلمة واحدة بس
cout << name;

getline(cin, name);    // بياخد جملة كاملة فيها مسافات هي هي نفس فكرة السي ان لكن الفرق انه لما تيجي تدوس مسافة في السي ان انت كانك دوست علي انتر لكن دي اكتب زي ما انت عايز لحد ما تدوس انتر
cout << name;
```
الخلاصة
> ⚠️ `cin >>` مع `string` بياخد **كلمة واحدة بس**. لو عايز جملة كاملة فيها مسافات استخدم `getline`.

===================================================================================================================================================================

### تحديد النوع التلقائي ⚡

**`auto`** — بيقول للـ compiler "اشتق النوع بنفسك من القيمة"
```cpp
auto x    = 5;               // int
auto y    = 3.14;            // double
auto z    = 3.14f;           // float
auto flag = true;            // bool
auto name = string("Ahmed"); // string ✅

// تحذير
auto wrong = "Ahmed";        // const char* مش string!
```

> 💡 في البداية استخدم الأنواع الصريحة عشان تتعلم. `auto` أداة بتستخدمها لما تبقى شايل.

==============================================================================================================================================================
### الفراغ 🕳️

**`void`** — 0 bytes، معناه "لا شيء"

مش بتعمل منه متغير، بس بتشوفه في الـ functions اللي مش بترجع قيمة.
```cpp
void printHello() {
    cout << "Hello!";
    // مفيش return 
    // هتعرف اكتر ف الدوال
}
================================================================================================================================================================

### الجدول الكامل 📊 
  احم يا رب يكون واضح  😗

| النوع | الحجم | النطاق / الدقة | محتاج `#include`؟ |
|---|---|---|---|
| `short` | 2 bytes | ±32,767 | ❌ |
| `int` | 4 bytes | ±2.1 مليار | ❌ |
| `long long` | 8 bytes | ±9.2 × 10¹⁸ | ❌ |
| `unsigned int` | 4 bytes | 0 → 4.2 مليار | ❌ |
| `float` | 4 bytes | ~7 خانة عشرية | ❌ |
| `double` | 8 bytes | ~15 خانة عشرية | ❌ |
| `long double` | 8-16 bytes | ~18-19 خانة | ❌ |
| `char` | 1 byte | حرف ASCII واحد | ❌ |
| `wchar_t` | 2-4 bytes | Unicode | ❌ |
| `bool` | 1 byte | true / false | ❌ |
| `string` | متغير | أي نص | ✅ `<string>` |
| `auto` | حسب القيمة | — | ❌ |
| `void` | 0 | — | ❌ |

---

### الخلاصة 🎯

هتستخدم 4 أنواع في 95% من الكود اللي هتكتبه:
  لان الباقي ما هي الا سعة تخزينية اكبر و يفضل تعرفهم يستحسن ياريت تعرفهم غير الاربعة دول

- **`int`** — للأرقام الصحيحة
- **`double`** — للكسور
- **`string`** — للنصوص
- **`bool`** — للشروط


